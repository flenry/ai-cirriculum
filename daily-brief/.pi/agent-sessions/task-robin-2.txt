Analyse findings and write the final verdict.

Read:
- FINDINGS.md — Usopp's full results
- PRD.md — requirements
- context/review.md if it exists — review criteria and severity scale
- test-results/screenshots/ if they exist — describe what you see per flow

Write handover/test.md:
```
# Handover: test
_[date] | v1_

## Decided
- [what passed, PRD requirements confirmed, coverage achieved]

## Rejected
- N/A

## Open
- [failures, PRD gaps, issues found — each with severity]

## Output
- Test results: X/Y passing
- PRD coverage: X/Y requirements verified
- New tests: tests/prd-derived/ (X files)
- Screenshots: test-results/screenshots/ (X files)
```

Write handover/review.md:
```
# Handover: review
_[date] | v1_

## Decided
- Verdict: [READY / NEEDS FIXES / BLOCKED]
- [key findings with severity]

## Rejected
- N/A

## Open
- [anything requiring a CR or do-todo pass]

## Output
- Evidence: [test results + screenshots + PRD gap count]
```

Verdict criteria:
- READY — app ran successfully + all PRD requirements verified + no critical/major issues
- NEEDS FIXES — app ran but minor issues only — use /do-todo to clear then re-run full-test
- BLOCKED — app failed to run OR critical/major issues — needs a CR or build re-run

Note: if the app failed to run entirely, verdict is always BLOCKED regardless of test results.

Then call move_to_review for any open test tasks.

FINDINGS: 
Original context: Fix the "no such table: briefs" crash in daily-brief by auto-creating the schema in createDb().

## Problem
`pnpm brief` crashes on a fresh install with:
```
LibsqlError: SQLITE_ERROR: no such table: briefs
```
Root cause: `pruneOldBriefs()` runs before the DB schema exists. The `briefs` table is never created automatically — it requires a manual `drizzle-kit push` step that was never documented or wired into the startup flow.

## Your Job (Option A — Proper Fix)
Auto-create the schema inside `createDb()` so the `briefs` table always exists before any DB operation runs. No manual migration step required.

## Files to Read First
- src/db/client.ts
- src/db/schema.ts
- src/db/store.ts
- src/db/prune.ts
- src/index.ts
- drizzle.config.ts
- package.json
- CLAUDE.md

## Exact Fix Required

### src/db/client.ts
After creating the drizzle client, run an idempotent `CREATE TABLE IF NOT EXISTS briefs (...)` that matches the schema defined in `src/db/schema.ts`.

Use the raw libsql client to execute the DDL before returning the drizzle instance. Something like:

```ts
await client.execute(`
  CREATE TABLE IF NOT EXISTS briefs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    generated_at TEXT NOT NULL,
    weather_json TEXT,
    hn_json TEXT,
    github_json TEXT,
    wikipedia_json TEXT,
    status_json TEXT NOT NULL
  )
`);
```

Make sure to:
- Read schema.ts first to confirm exact column names and types
- Make `createDb` async (returns `Promise<DrizzleDb>`) since libsql execute is async
- Update all callers of `createDb` in src/index.ts to await it
- Update any test files that call `createDb` directly

## After Making Changes
1. Run: `pnpm test` — all 55 tests must still pass
2. Run: `pnpm brief` — must complete without "no such table" error
   - If it reaches API calls and fails on network/env issues that's fine
   - The DB error must be gone
3. Add test: `tests/prd-derived/cli-fresh-db.test.ts`
   - Spawns the brief generator against a fresh `:memory:` DB
   - Asserts no "no such table" error
   - Asserts brief is generated (even with mocked/failed APIs)

## Constraints
- Do NOT change schema.ts column definitions
- Do NOT change store.ts, prune.ts, or any service/renderer files beyond import fixes
- Do NOT add drizzle-kit as a runtime dependency
- Keep all 55 existing tests passing

## When Done
1. Confirm `pnpm brief` runs past the DB error
2. Confirm test count is 56+ (55 existing + new cli-fresh-db test)
3. move_to_review with summary
