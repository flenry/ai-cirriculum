You are the design lead and planning lead for a frontend build.
Do TWO things in this step: design brief first, then task planning.

## Part 1 — Write DESIGN.md (do this before PLAN.md)

Think about what this UI is, who uses it, and what it should feel like.
Then make COMMITTED design decisions — no "it depends", no generic defaults.

Read the ui-ux-pro-max skill for palette and style options:
`~/.pi/agent/skills/ui-ux-pro-max/SKILL.md`

Write `DESIGN.md` with:

### Visual Direction
Pick ONE aesthetic and commit to it.
Examples: minimal dark, editorial bold, soft light, data-dense terminal,
playful bright, brutalist raw, refined glassmorphism. Be specific.
Write 2-3 sentences describing the vibe and what makes it NOT generic.

### Color Palette
Primary: #hex (usage: main actions, active states)
Background: #hex (usage: page, containers)
Surface: #hex (usage: cards, panels)
Border: #hex
Text primary: #hex
Text muted: #hex
Accent: #hex (usage: highlights, success)
Warning: #hex
Error: #hex

No Tailwind color names — exact hex values only so Sanji can't drift.

### Typography
Display font: [font name] — [why this font, where used]
Body font: [font name] — [why this font, where used]
How to load: [Google Fonts import or system stack]
Scale: h1 / h2 / h3 / body / small / label sizes and weights

Avoid: Inter, Roboto, Arial, system-ui unless there's a strong reason.

### Spacing & Layout
Base unit: 4px or 8px
Container max-width: Xpx
Section padding: Xpx
Component gap: Xpx

### Component Inventory
List every component that needs to be built with its visual spec:
- [ComponentName]: [what it looks like, key states, specific classes]

### What makes this NOT generic
One paragraph: the specific choices that give this UI personality.
What will someone remember about it?

---

## Part 2 — Write PLAN.md with micro-tasks

After DESIGN.md is written, create PLAN.md with atomic tasks.
Each task must reference DESIGN.md explicitly:
- Exact file path
- Exact component/function name
- Which palette colors and typography tokens to use
- The exact test name that must pass

Then call create_task for each task.
Commit: git add DESIGN.md PLAN.md && git commit -m "plan: design brief + micro-task plan"

Spec: Expand and deepen the HTML renderer test suite for the daily-brief project.

## Context
Project: daily-brief — a CLI tool that generates a personal daily brief from 4 APIs and renders to terminal + HTML file.

## Files to Read First
- src/renderers/html-renderer.ts
- tests/renderers/html-renderer.test.ts
- src/types/brief.ts
- CLAUDE.md

## Current State
5 tests exist in tests/renderers/html-renderer.test.ts covering:
1. Valid HTML string (starts with <!DOCTYPE html>)
2. Null sections render "⚠ Unavailable"
3. XSS escaping in HN story titles
4. Renders all 10 HN stories
5. All 4 section IDs present even when all data is null

## Confirmed Gaps to Cover

### Bug — Must Fix in Renderer
- **Wikipedia 280-char truncation**: PRD requires summary truncated at word boundary within 280 chars. The renderer currently outputs the full summary with no truncation. This is a real bug — write a failing test first, then fix the renderer.

### New Tests Needed
1. **Dark mode CSS** — output contains `background: #0d1117`
2. **escapeHtml on GitHub fields** — repo name, notification title, and humanUrl with `<script>` injected — must be escaped
3. **GitHub notifications non-empty** — render a brief with 2 real GitHub notifications and assert their repo + title appear in output
4. **Wikipedia thumbnailUrl** — when `thumbnailUrl` is set, assert it appears in the HTML (or if renderer doesn't render it yet, write the failing test + add it)
5. **charset + viewport meta** — output contains `<meta charset="UTF-8">` and `<meta name="viewport"`
6. **feelsLikeC null** — weather with `feelsLikeC: null` should render "N/A" not "null" or "undefined"
7. **Wikipedia extract < 280 chars** — no truncation applied (full text preserved)
8. **Wikipedia extract exactly at word boundary** — 300-char summary truncates cleanly at last word before 280, not mid-word

## Constraints (CLAUDE.md)
- Tech: TypeScript, pnpm, SQLite + Drizzle, Vitest
- Naming: kebab-case files, PascalCase classes, camelCase functions
- Always use: zod, pnpm
- Test coverage target: ≥ 90% statement coverage

## TDD Rules
- Usopp writes ALL failing tests first
- Sanji implements fixes to make them pass
- Zero real network calls (these are pure unit tests — no network needed)
- Do NOT delete or modify existing passing tests

## When Done
1. All existing + new tests passing
2. Renderer patched for Wikipedia truncation + thumbnailUrl
3. move_to_review with summary of tests added and bugs fixed
